default_platform(:ios)

lane :build_iAPS do
  build_app(
    workspace: "iAPS.xcworkspace",
    scheme: "iAPS",
    configuration: "Release",
    export_method: "ad-hoc"
  )
end

lane :release do
end
